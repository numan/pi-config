---
name: visually-test-branch
---

This workflow is launched by the local `/visually-test-branch` extension.

Read the provided run context first. Use the canonical run directory as the only source of truth for artifacts, keep every file for this run inside that directory, and continue the visually-test-branch workflow from there.
